DATABASE_URL=postgresql://beast:beast_dev_password@localhost:5432/beast

# Encryption key for secrets vault (generated by `make install`)
# Must be a 64-character hex string (32 bytes)
ENCRYPTION_KEY=
